#include "../inc/binary_search.hh"

/*
template <typename T>
int bin_search(T arr[],T element,int l, int r)
{
        int mid = l + (r-l)/2;
        if(l > r) return -1;

        if(arr[mid] == element){
            return mid;
        }
        else if(arr[mid] > element){
            return bin_search(arr,element,l,mid-1);
        }
        else{
            return bin_search(arr,element,mid+1,r);
        }
    
    
}

*/